{
  "name": "api-list-books",
  "version": "1.0.0",
  "description": "api fake list books",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3004"
  },
  "author": "Willy",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
